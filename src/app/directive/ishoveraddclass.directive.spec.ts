import { IshoveraddclassDirective } from './ishoveraddclass.directive';

describe('IshoveraddclassDirective', () => {
  it('should create an instance', () => {
    const directive = new IshoveraddclassDirective();
    expect(directive).toBeTruthy();
  });
});
